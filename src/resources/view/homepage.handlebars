<section class="hero-banner">
  <div id="heroCarousel" class="carousel slide" data-bs-ride="carousel">
    <div class="carousel-inner">
      <div class="carousel-item active">
        <img src="/img/Hero_Banner_-_Desktop_do_lot.webp" class="d-block w-100" alt="Banner 1" />
      </div>
      <div class="carousel-item">
        <img src="/img/Hero_Banner_-_Desktop_run.webp" class="d-block w-100" alt="Banner 2" />
      </div>
      <div class="carousel-item">
        <img src="/img/Hero_Banner_-_Desktop_udw.webp" class="d-block w-100" alt="Banner 3" />
      </div>
      <div class="carousel-item">
        <img src="/img/Hero_Banner_-_Desktopak.webp" class="d-block w-100" alt="Banner 4" />
      </div>
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#heroCarousel" data-bs-slide="prev"
      style="height:fit-content; width: 80px; top: 40%;">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="visually-hidden text-black">Trước</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#heroCarousel" data-bs-slide="next"
      style="width: 80px; height:fit-content; top: 40%;">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="visually-hidden">Sau</span>
    </button>
  </div>
</section>

<!-- Thời trang nam -->
<section class="categories-section">
  <div class="container-categories">
    <div class="category-sex">
      <h2 style="font-family: 'MightiestFont', sans-serif; font-weight: 500; font-size: 50px;">
        THỜI TRANG NAM
      </h2>
    </div>
    <div class="categories-wrapper">
      <button class="nav-btn prev-btn">←</button>
      <div class="categories-container">
        {{#each categories}}
        {{#if (eq this.name "Thời trang nam")}}
        {{#if this.children.length}}
        <div class="category-items">
          {{#each this.children}}
          <div class="category-item animate">
            <a href="/collection/{{this.slug}}">
              <img src="{{this.image}}" alt="{{this.name}}" class="img-fluid rounded-4" />
            </a>
            <p class="mt-2" style="font-family: NorthCarossela; font-size: 25px; font-weight: 100;">
              {{this.name}}
            </p>
          </div>
          {{/each}}
        </div>
        {{else}}
        <p>Không có danh mục con nào để hiển thị.</p>
        {{/if}}
        {{/if}}
        {{else}}
        <p>Không có danh mục nào để hiển thị.</p>
        {{/each}}
      </div>
      <button class="nav-btn next-btn">→</button>
    </div>
  </div>
</section>

<section class="banner-block">
  <div class="container-fluid px-0">
    <div class="banner position-relative">
      <img src="/img/Do_thu_dong_-_Desktop.webp" alt="Banner Đồ Thu Đông" class="img-fluid w-100" />
      <!-- Overlay nội dung -->
      <div class="banner-overlay position-absolute text-start text-white" style="bottom: 80; left: 80">
        <h2 class="display-4 fw-bold text-black" style="font-family: MightiestFont, sans-serif; font-size:100px">ĐỒ THU
          ĐÔNG</h2>
        <p class="lead" style="font-size: 30;">
          Giảm đến 50% | Nhập "CM10" giảm thêm 10% | Freeship từ đơn 200k
        </p>
        <a href="#" class="btn btn-primary d-inline-flex align-items-center">
          Mua Ngay
          <span class="ms-2">&rarr;</span>
        </a>
      </div>
    </div>
  </div>
</section>

<section class="categories-section">
  <div class="container-categories">
    <div class="category-sex">
      <h2 style="font-family: 'MightiestFont', sans-serif; font-weight: 500; font-size: 50px;">
        THỜI TRANG NỮ
      </h2>
    </div>
    <div class="categories-wrapper">
      <button class="nav-btn prev-btn">←</button>
      <div class="categories-container">
        {{#each categories}}
        {{#if (eq this.name "Thời trang nữ")}}
        {{#if this.children.length}}
        <div class="category-items">
          {{#each this.children}}
          <div class="category-item animate">
            <a href="collection/{{this.slug}}">
              <img src="{{this.image}}" alt="{{this.name}}" class="img-fluid rounded-4" />
            </a>
            <p class="mt-2" style="font-family: NorthCarossela; font-size: 25px; font-weight: 100;">
              {{this.name}}
            </p>
          </div>
          {{/each}}
        </div>
        {{else}}
        <p>Không có danh mục con nào để hiển thị.</p>
        {{/if}}
        {{/if}}
        {{else}}
        <p>Không có danh mục nào để hiển thị.</p>
        {{/each}}
      </div>
      <button class="nav-btn next-btn">→</button>
    </div>
  </div>
</section>